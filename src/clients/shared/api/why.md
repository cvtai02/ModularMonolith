## Claude just need to know:
ApiClientInterfaces - the Abtractions:
1. method
2. the input --> read backend dtos folder for details
3. the output --> read backend dtos folder for details
--> read permission for contracts folder

## Claude not allow to know:
1. implementation of api clients --> deny read permission for shared/api/clients
2. types generated by openapi-typescript cause the file is too large --> deny read permission for shared/api/lib
3. type because it's just rename of types generated by openapi-typescript --> deny read permission for shared/api/type-names


### contracts folder export Interface for Api Client and types.
### types folder is just a rename of types generated by openapi 
### clients folder is implementation of contracts
